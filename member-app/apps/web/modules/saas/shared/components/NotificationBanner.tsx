"use client";

/**
 * NotificationBanner component - Currently disabled
 *
 * All notifications are displayed in the notification dropdown menu
 * with a badge showing the unread count. No banner notifications
 * are shown to avoid cluttering the interface.
 *
 * To re-enable banners, restore the component logic and update
 * the layout file to render this component.
 */
export function NotificationBanner() {
	// Banners disabled - all notifications appear in dropdown only
	return null;
}
